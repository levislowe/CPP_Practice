#include <iostream>
using namespace std;

int main() {
    int time = 20;
    if (time < 18) { //block of code to be executed if the condition is true
        cout << "Good day." << endl;
    } else { //block of code to be executed if the condition is false
        cout << "Good evening." << endl;
    }

    return 0;
}